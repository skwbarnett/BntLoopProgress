## BntLoopProgress
可配置环形进度条
![](/preview/s.png)


```
使用CAShapeLayer作为superLayer，添加CAGradientLayer分段制作渐变色背景，
添加UIBezierPath绘制的环形CAShapeLayer 的mask，作为上层遮罩
同时为mask添加POPSpringAnimation动画
```

~~~
Use CAShapeLayer as superLayer, 
add CAGradientLayer segmented gradient background,
add the drawings of the annular CAShapeLayer UIBezierPath mask, as the upper layer maskAt the same time add POPSpringAnimation animation to mask

